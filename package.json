{
  "name": "job-scout-frontend",
  "version": "0.0.0",
  "private": true,
  "description": "Frontend test harness for the job-scout jobs page.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@vitest/coverage-v8": "^2.1.4",
    "jsdom": "^25.0.1",
    "vitest": "^2.1.4"
  }
}
